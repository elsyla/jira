# configurations (files, groovy scripts, etc) that are common for all projects
# the 'scripts' folder is deployed to jira server's /opt/atlassian/jira-data/scripts
# which is symlinked to /opt/atlassian/jira/atlassian-jira/WEB-INF/classes/com/
