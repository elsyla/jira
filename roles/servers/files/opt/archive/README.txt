due to large size (>200MB) of Jira app package, we don't keep it in git repo
instead, we download it one time in checked out git repo and run ansible the
first time (at least one time) manually for new server(s). 

download location: https://www.atlassian.com/software/jira/download
